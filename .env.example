# Client Portal uses relative /api and /static paths.
# Production Vercel routing is configured in vercel.json and proxies these
# paths to https://api.buykori.app.
#
# No frontend secret is required for the current portal build.
