# Buykori Client Portal

Vercel-ready static frontend for `client.buykori.app`.

Set the Vercel project root to `client-portal`.

Runtime API target:

```txt
https://api.buykori.app
```

The first version uses the client's API key in the browser and calls the existing authenticated client APIs with `X-API-Key`.
